# Test2 Repository

This repository now includes a simple graphical calculator implemented in Python using `tkinter`.

## Running the calculator

Run the following command in your terminal:

```bash
python calculator_gui.py
```

This will open a window with an advanced calculator interface supporting basic arithmetic operations as well as functions like `sin`, `cos`, `tan`, `sqrt`, and `log`.
